spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    show-sql: false

  data:
    redis:
      host: localhost
      port: 6379

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

# ===== Dummy values =====
auth:
  signer-key: test
  valid-duration: 86400
  refreshable-duration: 259200

cloudinary:
  cloud-name: test
  api-key: test
  api-secret: test

app:
  admin:
    email: admin@test.com
    password: 123456
  librarian:
    email: lib@test.com
    password: 123456
  mail:
    host: localhost
    port: 1025
    username: test
    password: test