# name: CI

# on:
#   pull_request:
#     branches:
#       - main
#     types:
#       - closed

# env:
#   JAVA_VERSION: '21'
#   BACKEND_CONTEXT: ./oulib-backend
#   FRONTEND_CONTEXT: ./oulib-frontend

# jobs:
#   backend-tests:
#     name: Backend Tests
#     runs-on: ubuntu-latest
#     if: github.event.pull_request.merged == true && github.event.pull_request.head.ref == 'develop'

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
#         with:
#           ref: ${{ github.event.pull_request.merge_commit_sha }}

#       - name: Set up JDK
#         uses: actions/setup-java@v4
#         with:
#           distribution: temurin
#           java-version: ${{ env.JAVA_VERSION }}
#           cache: maven

#       - name: Run backend tests
#         working-directory: ${{ env.BACKEND_CONTEXT }}
#         run: mvn -B clean test

#   frontend-build:
#     name: Frontend Build
#     runs-on: ubuntu-latest
#     if: github.event.pull_request.merged == true && github.event.pull_request.head.ref == 'develop'

#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-node@v4
#         with:
#             node-version: 22
#             cache: npm
#             cache-dependency-path: oulib-frontend/package-lock.json  

#       - run: npm ci
#         working-directory: oulib-frontend  
#       - run: npm run build
#         working-directory: oulib-frontend

#   build-and-push:
#     name: Build and Push Images
#     runs-on: ubuntu-latest
#     needs: [backend-tests, frontend-build]
#     if: github.event.pull_request.merged == true && github.event.pull_request.head.ref == 'develop'

#     permissions:
#       contents: read

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
#         with:
#           ref: ${{ github.event.pull_request.merge_commit_sha }}

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Login to DockerHub
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.DOCKERHUB_USERNAME }}
#           password: ${{ secrets.DOCKERHUB_TOKEN }}

#       - name: Docker metadata (backend)
#         id: backend_meta
#         uses: docker/metadata-action@v5
#         with:
#           images: ${{ secrets.DOCKERHUB_USERNAME }}/oulib-backend
#           tags: |
#             type=raw,value=latest
#             type=sha

#       - name: Docker metadata (frontend)
#         id: frontend_meta
#         uses: docker/metadata-action@v5
#         with:
#           images: ${{ secrets.DOCKERHUB_USERNAME }}/oulib-frontend
#           tags: |
#             type=raw,value=latest
#             type=sha

#       - name: Build and push backend image
#         uses: docker/build-push-action@v6
#         with:
#           context: ${{ env.BACKEND_CONTEXT }}
#           file: ${{ env.BACKEND_CONTEXT }}/Dockerfile
#           push: true
#           tags: ${{ steps.backend_meta.outputs.tags }}
#           labels: ${{ steps.backend_meta.outputs.labels }}
#           cache-from: type=gha
#           cache-to: type=gha,mode=max

#       - name: Build and push frontend image
#         uses: docker/build-push-action@v6
#         with:
#           context: ${{ env.FRONTEND_CONTEXT }}
#           file: ${{ env.FRONTEND_CONTEXT }}/Dockerfile
#           push: true
#           build-args: |
#             VITE_API_BASE_URL=${{ vars.VITE_API_BASE_URL || 'http://localhost:8080' }}
#           tags: ${{ steps.frontend_meta.outputs.tags }}
#           labels: ${{ steps.frontend_meta.outputs.labels }}
#           cache-from: type=gha
#           cache-to: type=gha,mode=max
